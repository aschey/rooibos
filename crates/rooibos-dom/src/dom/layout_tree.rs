// use taffy::{AvailableSpace, NodeId, Size, TaffyTree};

// pub(crate) struct LayoutTree(TaffyTree<()>);

// impl LayoutTree {
//     pub(crate) fn compute_layout(&mut self, node: NodeId, available_space: Size<AvailableSpace>)
// {         self.0.compute_layout(node, available_space);
//         let location = self.0.layout(node).unwrap().location;
//     }
// }
